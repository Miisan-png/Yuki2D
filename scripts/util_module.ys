fn greet(name) {
    print("Hello from module, " + name);
}

fn get_message() {
    return "Imported message body.";
}

var util = map();
map_set(util, "greet", greet);
map_set(util, "get_message", get_message);
exports = util;
