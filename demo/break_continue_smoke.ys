fn init() {
    var i = 0;
    var sum = 0;
    while (i < 10) {
        i = i + 1;
        if (i == 3) continue;
        if (i == 8) break;
        sum = sum + i;
    }
    assert(sum == (1 + 2 + 4 + 5 + 6 + 7), "break/continue failed");
    print("break/continue ok:", sum);
}

fn update(dt) {
}
